{
  "name": "merge-your-music",
  "version": "1.0.0",
  "description": "A webapp for generating playlists that mix your music taste with someone else's.",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsc -w & nodemon build/index.js --ignore client",
    "build-all": "npx tsc && cd client && npm run build",
    "build": "npx tsc",
    "prod": "node build/index.js"
  },
  "author": "Tatu Laras",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "express-session": "^1.17.3",
    "express-ws": "^5.0.2",
    "nodemon": "^3.0.3",
    "react": "^18.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  },
  "devDependencies": {
    "@tsconfig/node21": "^21.0.1",
    "@types/node": "^20.11.9"
  }
}
